# Migrated TalentFlow Backend (Blueprint/App Factory)

Original monolith saved at `app/legacy/backend_original.py`.
Routes transformed into a Blueprint at `app/routes/api.py`.

## Run
```bash
pip install -r requirements.txt
python run.py
```
